


const scriptsInEvents = {

	async Menu_Event4_Act1(runtime, localVars)
	{
		if (typeof sdk !== 'undefined' && sdk.showBanner !== 'undefined') {
		sdk.showBanner();
		}
	},

	async Game_Event296_Act1(runtime, localVars)
	{
		if (typeof sdk !== 'undefined' && sdk.showBanner !== 'undefined') {
		sdk.showBanner();
		}
	}

};

self.C3.ScriptsInEvents = scriptsInEvents;

